# -------------------------------------------------------------------
# Louhen Landing Environment Template
# -------------------------------------------------------------------
# This file documents every environment variable used by louhen-landing.
# It must NEVER contain real credentials. Copy it to `.env.local` for local
# development and mirror the documented values into Vercel (Preview/Prod)
# and GitHub Actions as described in /CONTEXT/envs.md and README.
# -------------------------------------------------------------------

# ===== Public (client-visible) values =====
NEXT_PUBLIC_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=10000000-ffff-ffff-ffff-000000000001
NEXT_PUBLIC_WAITLIST_URGENCY=true
NEXT_PUBLIC_BANNER_WAITLIST_URGENCY=true
NEXT_PUBLIC_ANALYTICS_ENABLED=0
NEXT_PUBLIC_ANALYTICS_DISABLED=1
NEXT_PUBLIC_ANALYTICS_DEBUG=0
NEXT_PUBLIC_LOCALES=en-de,de-de
NEXT_PUBLIC_DEFAULT_LOCALE=en-de
NEXT_PUBLIC_ACCESS_REQUEST_URL=/onboarding/request-access
NEXT_PUBLIC_APP_DOWNLOAD_URL=/download
NEXT_PUBLIC_DASHBOARD_URL=/dashboard
NEXT_PUBLIC_LOGOUT_URL=/logout
NEXT_PUBLIC_CSP_REPORT_ONLY=0
NEXT_PUBLIC_ALLOW_INDEXING=false
# Feature experiments (leave unset unless actively used)
# NEXT_PUBLIC_PROMO_RIBBON_ENABLED=0
# NEXT_PUBLIC_PROMO_RIBBON_ID=
# NEXT_PUBLIC_PROMO_RIBBON_LINK=
# NEXT_PUBLIC_CANONICAL_HOST=www.louhen.app
# NEXT_PUBLIC_HEADER_PHASE=waitlist

# ===== Core application runtime =====
APP_BASE_URL=http://localhost:3000
IS_PRELAUNCH=true
EMAIL_TRANSPORT=noop
ANALYTICS_STORE_IP=false
ANALYTICS_IP_SALT=test-analytics-salt
OG_DYNAMIC_ENABLED=true
SECURITY_REPORT_ONLY=true
COMMIT_SHA=local-dev

# ===== Firebase Admin (server only) =====
FIREBASE_ADMIN_SA_B64=__base64_encoded_service_account__
FIREBASE_PROJECT_ID=louhen-dev
FIREBASE_DB_REGION=eur3
# Optional legacy JSON form (prefer FIREBASE_ADMIN_SA_B64)
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"louhen-dev"}'

# ===== hCaptcha =====
HCAPTCHA_SECRET=0x0000000000000000000000000000000000000000

# ===== Email delivery (Resend) =====
RESEND_API_KEY=dev-noop-key
RESEND_FROM=no-reply@louhen.app
RESEND_REPLY_TO=hello@louhen.app

# ===== Waitlist + rate limiting =====
WAITLIST_CONFIRM_TTL_DAYS=7
WAITLIST_RATE_SUBMITS_PER_HOUR_PER_IP=10
WAITLIST_RATE_RESENDS_PER_30M_PER_EMAIL=3
WAITLIST_RATE_HASH_SECRET=rate-limit-secret
SUPPRESSION_SALT=unsubscribe-secret
EMAIL_HASH_SALT=test-email-hash-salt

# ===== Test & CI harness =====
TEST_MODE=0
TEST_E2E_SHORTCIRCUIT=true
TEST_E2E_BYPASS_TOKEN=e2e-mocked-token
PLAYWRIGHT_ARTIFACTS_DIR=artifacts/playwright/local
BASE_URL=http://127.0.0.1:4311
HOST=0.0.0.0
PORT=4311
PW_HEALTH_PATH=/icon.svg
PW_FAILURES_MD=artifacts/playwright/failures.md

# ===== Diagnostics & admin tooling =====
STATUS_USER=status-ops
STATUS_PASS=status-secret
ADMIN_KEY=dev-admin-token

# ===== Preview protection & external integrations =====
# Set these exclusively in GitHub/Vercel secrets (never commit real values)
# PREVIEW_BYPASS_TOKEN=set-via-github-actions-secret
# PROTECTION_HEADER="X-Bypass-Token: ${PREVIEW_BYPASS_TOKEN}"
# PROTECTION_COOKIE=sessionid=example
# VERCEL_AUTOMATION_BYPASS_SECRET=rotate-me

# ===== Optional script controls =====
# DRY_RUN=1
# BATCH_SIZE=400
# CONFIRM_URL=https://preview.louhen.app/waitlist/confirm
